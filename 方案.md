# 网球自动捡球机器人 — 技术方案

**版本**：v1.3（对齐工程 E1.3：样品即无刷 · 前轮转向+后通轴 · 滚轮抛高入筐）

## 1. 目标与约束

### 功能目标
1. 在网球场内**识别多颗网球**（含不同颜色品种）
2. 依次前往捡球，**结束后自动返回起点**
3. 底盘移动、捡球机构执行、基础避障
4. **以速度优先**：尽量少停车，捡球过程不中断行进
5. **抛高入筐**：滚轮不只要咬球，还要把球抛起撞挡板进入球筐

### 工程约束
| 约束 | 说明 |
|------|------|
| 成本 | 不上树莓派；上位机用低成本 Linux 小板 |
| **动力** | **滚轮 + 后驱样品起全部无刷**（商品化同一平台，不做有刷过渡样机） |
| 速度 | 巡航与捡球阶段保持较高速度，避免“停-捡-走” |
| 场地 | 网球场环境较复杂，可能存在近似颜色干扰物 |
| 球色 | 荧光黄（标准）、训练橙球等，不能假设单一颜色 |
| 实时性 | 多球检测需稳定帧率，决策不依赖云端 |

### 技术路线结论
- **双机架构**：上位机（香橙派 Zero 2W）负责视觉与决策；下位机（ESP32）负责运动与机构
- **识别策略**：不用纯色块；采用 **多色 HSV + 圆度 + 尺寸 + 连续帧** 的组合约束
- **捡球机构**：筋尖进料让位 + **进料后挡咬合** + 上升段抛送；滚轮名义 **≈450 RPM（Ø152）** 抛入筐
- **轮系**：前舵机转向 + 后单电机通轴；里程计=驱动轴+转角
- **回起点（E1.3c）**：**可搬信标柱** 即家；standoff **≥35 cm**（>D_hard）；不以编码器 (0,0) 为成功判据
- **距离感知**：**ToF/超声防撞限速 + 单目测球距**；第一期不上深度相机
- **安全硬指标**：**不能撞墙**（L1 本地急停，不依赖视觉）
- **轮系（E1.2/E1.3）**：**前轮中置舵机转向（±30°）+ 后轮单电机通轴驱动**；里程计用**驱动轴编码器 + 前轮转角**推算（**不是**左右轮差速）。详见 `engineering-design.md` §6
- **回起点（E1.3c）**：**可搬信标柱** 即家；RETURN 找柱并伺服；**standoff≥35 cm**；里程计不作成功判据
- **容量**：样品标称 **30 球**；成品容量分级（见工程文 §5.5）

---

## 2. 总体架构

```text
                    ┌─────────────────────────┐
                    │   上位机 Orange Pi       │
                    │   Zero 2W (Linux)        │
                    │                          │
   USB Camera ─────►│  感知：多球检测/单目测距  │
                    │  决策：捡球顺序/回起点    │
                    │  限速：v_allowed(d_front) │
                    └───────────┬─────────────┘
                                │ USB 串口 (高速)
                    ┌───────────▼─────────────┐
                    │   下位机 ESP32           │
                    │                          │
                    │  电机驱动 / 编码器        │
                    │  滚轮捡球机构             │
                    │  ToF×2–3 防撞硬急停       │
                    │  （+可选超声）            │
                    └─────────────────────────┘
```

### 职责划分

| 层级 | 硬件 | 职责 | 不负责 |
|------|------|------|--------|
| 感知/决策 | Orange Pi Zero 2W | 图像采集、多球检测跟踪、单目测球距、目标排序、按 ToF 计算限速、**信标搜索与视觉回家** | 直接驱动电机、本地急停 |
| 执行/安全 | ESP32 | 速度环、编码器里程计、滚轮、**ToF 采样与 D_hard 急停** | 复杂视觉 |

**原则**：上位机挂了，下位机仍能停机/保持安全；下位机不依赖上位机做碰撞保护。

---

## 3. 硬件选型（BOM 建议）

### 3.1 计算与感知

| 模块 | 推荐型号 | 大概价位 | 说明 |
|------|----------|----------|------|
| 上位机 | 香橙派 Zero 2W（1–2GB） | ¥100–180 | 跑 OpenCV 组合检测足够 |
| 摄像头 | USB 免驱 UVC 摄像头（640×480） | ¥30–80 | 广角稍大更利于扫场 |
| 下位机 | ESP32-WROOM / ESP32 DevKit | ¥20–40 | 电机、编码器、串口、机构 |
| **电机驱动** | **无刷驱动 ×2（或一体驱动）**，每路持续 ≥3 A | ¥0–80（若电机含驱动） | **禁止 TB6612 驱无刷** |
| **后驱电机** | **无刷减速 12V ≈350 RPM + 编码/霍尔** | ¥150–220 | 单电机通轴；1:1 带轮 |
| 捡球机构 | **前滚轮 × 后弧板咬合** + 弧道上升 + 挡板入筐 | 见工程文 | 行进中收集，高速抛送 |
| **滚轮电机** | **无刷 ≈450–500 RPM + 驱动** | ¥120–180 | 20T:20T；名义 450 RPM（Ø152）；轮端≥5 kg·cm |
| **避障测距（主）** | **ToF：VL53L1X ×3**（前、左前、右前） | ¥90 | 硬限速/急停；注意 I2C 分地址 |
| 避障测距（辅） | 超声波 HC-SR04 ×1–2（可选） | ¥10–20 | 与 ToF 互补 |
| 供电 | **3S 15000 mAh** + BEC；**弹仓易换** | — | 3 年后 ≥2 h；XT90；保险 25–40 A |
| 通信 | USB 线（Pi ↔ ESP32） | — | 口不够则 USB Hub |

> 若 Zero 2W 的 USB 口不够：上 USB Hub，或摄像头选 CSI 排线款（需确认香橙派对应接口与驱动）。

### 3.2 距离感知分层（已选定：ToF/超声 + 单目，不上深度相机）

| 层 | 手段 | 负责 | 不负责 |
|----|------|------|--------|
| L1 硬安全 | 前/左前/右前 **ToF**（+可选超声） | 墙、围网、柱子距离；**硬限速/急停** | 认出是什么障碍 |
| L2 球距 | **单目 + 已知球径 6.7 cm** | 球的远近、相对滚轮入口横向偏差 | 前方空旷程度 |
| L3 粗空闲 | 视觉地面/色域（可选增强） | 远处大致是否开阔 | 不能当硬安全 |

**硬性要求：不能撞墙。** 撞墙防护以 L1 为准，不依赖摄像头是否看见网/柱。

ToF 布置建议：
- **正前 1 路**：主刹停与 TRANSIT 限速
- **左前、右前各 1 路**（约 ±30°）：侧向擦墙、转弯贴边
- 安装高度对齐围网/挡墙主要立面；避免被滚轮支架挡住
- 第一期可先 1 路正前调通限速，再补侧向

单目测球距：
- 标定：已知球径 + 像素直径 → 距离表/公式（一次现场标定）
- 接近段用该距离做减速与对准；不要求厘米级，要求稳定单调

### 3.3 为何不用树莓派
- 多球色块级检测 + 轻量规划，Zero 2W 算力够
- 成本约为树莓派 4B/5 方案的 1/2 左右
- 后续若要上小 CNN / YOLO，可平滑升级 Orange Pi 3B/5 或二手树莓派 4B，架构不变

---

## 4. 感知：多球识别策略

**不用纯色块。** 默认流水线如下（全部 OpenCV，可在 Zero 2W 上跑）。

### 4.1 流水线

```text
采集帧 (640×480，目标 **≥20 FPS**；速度优先时可 320×240 换帧率)
    → 降采样/ROI（可只看地面区域）
    → 多组 HSV 阈值（荧光黄 / 橙 / 白 等）
    → 形态学开运算去噪
    → 轮廓提取
    → 几何过滤：圆度、外接圆、面积/像素直径
    → 颜色类别标注
    → 多目标跟踪（帧间关联）
    → 输出：球列表 [id, color, image_pos, confidence, last_seen]
```

### 4.2 各约束的作用

| 约束 | 作用 | 对付什么 |
|------|------|----------|
| **多组 HSV** | 支持荧光黄、橙、白等多品种 | “网球也有多种颜色” |
| **圆度 / 外接圆** | 轮廓接近正圆才保留 | 黄色衣物、线缆、纸片等非球体 |
| **像素尺寸范围** | 按安装高度限制直径上下限 | 远处小黄噪点、近处大面积色块 |
| **连续帧确认** | 连续 N 帧（如 3–5）位置平滑才确认 | 闪烁高光、瞬间遮挡误检 |

### 4.3 参数要点（初值，需实测标定）

- HSV 按现场光线下采样板球；建议现场用滑条调参一次，存配置文件
- 荧光黄大致落在 H 约 25–40（OpenCV 0–179）、高 S、高 V；**必须实测，不可死抄**
- 圆度：`4πA / P²` 阈值约 0.65–0.8（视距离与畸变）
- 安装：摄像头朝前下倾，保证地平线在画面上部，减少天空/围网干扰

### 4.4 速度优先对感知的要求
- **提前量**：车速升高时，需在更远处确认球（增大有效检测距离），给转向留时间
- **帧率**：巡航阶段优先保帧率；可动态降分辨率
- **ROI**：默认看车前地面扇形区域，减少无效像素
- **确认帧数**：低速可用 5 帧；高速巡航可降到 3 帧并提高跟踪器门限，避免漏跟

### 4.5 仍不够时的升级路径（本方案不作为第一期）

1. 加入简单纹理/边缘软硬度（毛毡球 vs 光滑塑料）
2. 背景差分（低速移动时）
3. 极轻量量化 CNN 二分类（是球/不是球）；需更强上位机

---

## 5. 决策与导航

### 5.1 坐标系
- **起点**为原点 `(0,0)`，车头初始朝向为 +X 或 +Y（固定一种）
- **里程计（E1.2/E1.3）**：**驱动轴编码器 + 前轮转角**推算 `(x, y, θ)`（后轮单电机通轴，**不是**左右轮差速）
- 相机检测得到的球，在车体系下的方位与距离估计 → 投影到世界系

### 5.2 捡球任务状态机（滚轮×弧板 · 行进中收集 + 抛送入筐）

```text
IDLE
  → SEARCH（原地/慢速旋转扫描，滚轮可预启）
  → ACQUIRE（锁定已确认球）
  → APPROACH（保持巡航速度接近，瞄准进料口投影点）
  → COLLECT_ON_THE_FLY（球过轮下 → 弧板处咬合上行 → 高速抛入筐；车不停）
  → VERIFY（视野中球消失，或球仓计数/开关反馈；失败则回 SEARCH 重试）
  → 路径上下一颗球更近？ 
        是 → 连续插入下一目标（不回 SEARCH）
        否 → SEARCH / 下一目标
  → 全部球处理完？
        是 → RETURN_HOME（较高速度回 (0,0)）
        否 → 继续
  → DONE
```

收集物理（与工程文 §0.4b 一致）：
- **COLLECT 不是停车状态**，与 APPROACH 合并为边走边收
- 咬合在**滚轮 × 后方弧板**，不在滚轮正下方；轮底默认高于球径仅为进料让位
- 出口近竖直上冲撞 60° 挡板（梁 y=520）入筐 → 滚轮按 **Ø152 / ≈450 RPM** 设计
- 进料：筋尖离地 68 让位 → 撞**咬合弧板（底缘 y=35=底盘底，硬约束）**→ 楔入咬合；弧板/弧道固定车架  
- **调节（E1.3e/f）**：只动滚轮滑架 → 改 C 与筋尖高度 → 咬合力度；**弧板底缘不得高于 35 mm**，否则球从板下漏过无法咬合
- VERIFY 失败允许低速回扫一次，再丢弃该球 ID

### 5.3 多球顺序与路径（速度向）
- 第一期：**最近邻贪心**
- 升级：对视野内多球做**共线/近线串联**——一次通过连收多颗，显著提升速度
- 滚轮连续工作时，规划上优先“能扫过的走廊”，减少急转

### 5.4 回起点（信标柱 · E1.3c）

**定义：** “起点/家”= **人工放置的可搬信标柱**，不是里程计里写死的坐标。连续捡球后编码器累计漂移不可避免；回家靠**看见柱子再精接近**。挪动信标即挪动“家”。

#### 信标柱规格（检测相关）

| 项 | 要求 |
|----|------|
| 主色 | **青蓝**（备用品红）；**避开**荧光黄/训练橙 HSV |
| 形状 | 竖直圆柱 + 加大底座；运输卡在车尾 |
| 尺寸 | 直径约 **Ø80–120 mm**，高约 **200–300 mm**（初值，样机可调） |
| 标记 | 侧面一圈 **AprilTag/ArUco**（边长约 40–60 mm）；可选顶部小码或双色环 |
| 携带 | 车尾/筐侧卡座；**开局由人放在期望的家**，任务中不固定在车上 |

#### 检测策略（复用现有流水线）

1. **远距离粗搜**：信标主色 HSV + 竖向长宽比/圆度 + 像素尺寸 + 连续 N 帧  
2. **近距离确认**：柱身 **AprilTag** → 唯一 ID，抑制包/衣物/水瓶同色误检  
3. **网球分离**：球贴地且色不同；信标 ROI 可偏画面上部，与找球 ROI 可分开  
4. **失败态**：倒柱/遮挡导致无确认 → SEARCH → 仍无则 **SAFE_STOP**（灯/蜂鸣），**禁止**报“已到家”

#### RETURN 状态机

```text
RETURN_HOME
  R1 粗导航
       朝「最后已知信标方位 / 场地偏置」行驶
       ToF 限速与 D_hard 硬急停仍最高优先级
       转弯宜温和，降低二次漂移
  R2 搜索信标
       靠近原区域或超时后降速
       前视扫描信标色；连续 N 帧通过才进入接近
       未检出 → 原地/扇形/螺旋扩大搜索
  R3 接近（视觉伺服）
       颜色粗跟踪；有 AprilTag 则 tag 精跟踪
       bearing 误差 → 转向；像素尺寸/tag 距离 → 减速
       禁止长距离纯里程计盲“走到记忆坐标”
  R4 到位
       停在柱前 **standoff ≥35 cm**（> D_hard 25 cm，相对车头/进料口）
       可选对准朝向；蜂鸣/状态灯 = 到家成功
  任一步超时/丢失
       → SAFE_STOP + 日志上报，人工接管
```

#### 任务模式与“家”

| 模式 | 行为 |
|------|------|
| 清场验收 | 捡完已确认球 → RETURN；**以信标 standoff 停车为通过标准** |
| 随出随捡 | 满筐/人工停 → RETURN 到**当前**信标位置 |
| 挪家 | 人把柱放到新位置即可；不依赖旧世界坐标 |

#### 与里程计的关系

| 用途 | 是否依赖绝对坐标 |
|------|------------------|
| 选球贪心、路径偏置 | 可用（允许分米级误差） |
| 接近信标/球 | **否**，相机闭环 |
| 回家成功判据 | **否**，检出信标并 standoff 停车 |
| 航向辅助（可选） | IMU 二期；样品不强制 |

**仍要做：** 限加速度/角速度、编码器线数、软场地低速——用于少乱走，**不能替代信标**。  
**样品不做：** 全场 SLAM；四路相机回家；无信标检出却报 DONE。

---

### 5.5 避障与安全（不能撞墙 · 最高优先级）

**目标：全速段均不发生与墙/围网/柱子的碰撞。**

#### 速度与距离规则（上位机 + 下位机双保险）

```text
# d_front = 正前 ToF；d_left / d_right = 侧前 ToF
# V_TRANSIT ≈ 1.2–1.5 m/s；a_brake 取实测，初值 1.0–1.5 m/s²

v_allowed = min(
    V_TRANSIT,
    sqrt(2 * a_brake * max(0, d_front - D_margin))
)
# D_margin：车体半宽/突出 + 传感盲区 + 余量，建议 15–25 cm，需实测

if d_front < D_hard (如 25–35 cm):
    下位机硬急停 / 微退          # 不依赖上位机
elif d_front < D_slow (如 0.8–1.2 m):
    强制降速并禁止加速
if d_left 或 d_right 过近:
    限制向该侧转向 / 反向偏置
```

#### 职责

| 层级 | 行为 |
|------|------|
| ESP32 | 20–50 Hz 读 ToF；低于 `D_hard` **立即**切断前进速度（看门狗同级）；上报距离 |
| Orange Pi | 用 `d_front` 做 `v_allowed`，规划绕行偏置；上位机失联时下位机仍能停 |
| 机械 | 车头可加轻量防撞条/缓冲，减轻残余冲击 |

#### 已知传感器局限与对策

| 局限 | 对策 |
|------|------|
| ToF 对强光/深色/斜面偶发失效 | 多路交叉验证；短时突变用中值滤波；异常则按最近有效值保守限速 |
| 超声波对软物/斜网可能“穿透”感 | 以 ToF 为主，超声只作冗余 |
| 滚轮/球仓遮挡光路 | 布点避开结构件；装配后做遮挡检查 |
| 高速时 50 Hz 采样仍可能偏晚 | `v_allowed` 按最坏延迟留 margin；`D_hard` 不可调得过小 |

#### 捡球与避障关系

- 滚轮与行进**不解锁停车**，但可限制最大转弯角速度，防止吸入失败
- 避障急停时滚轮可保持低速或延时关闭，避免堵球
- **任何任务状态不得覆盖 L1 急停**（含 RETURN_HOME、COLLECT）

### 5.6 变速档位（配合 ToF clear distance）

| 档位 | 触发 | 速度 |
|------|------|------|
| TRANSIT | 空跑/回程，且 `d_front` 足够 | **1.2–1.5 m/s**（无刷后驱理论≈1.65，受 `v_allowed` 钳制） |
| COLLECT | 滚轮工作中、有目标球 | 0.5–0.8 m/s |
| APPROACH | 球距 &lt; 1.5–2 m（单目估距） | 0.3–0.5 m/s |
| BRAKE | `d_front` 进入减速区 | 梯形降速至停 |

空跑「看到前方空就提速」以 **ToF 的 d_front** 为准，不用摄像头猜 10 m 空旷。

---

## 6. 下位机（ESP32）设计

### 6.1 功能
- **无刷驱动**接口：PWM+DIR（或厂商协议）；后驱编码器计数
- 速度环（PI）与基础航向保持
- 里程计积分（**驱动轴 + 前轮转角**），周期上报位姿
- 滚轮电机：启停 + PWM（名义 **≈450 RPM**，400–520 标定）
- 存球仓：可选对射红外/微动开关计数
- **ToF ×3（+可选超声）采样、中值滤波、D_hard 急停**（本地完成，不依赖上位机）
- 解析串口指令并回报状态

### 6.2 控制频率建议
- 速度环：100–200 Hz
- 里程计上报：20–50 Hz
- **ToF/避障采样：50 Hz 优先**（超声可略低）
- 串口指令处理：事件驱动 + 周期状态包

---

## 7. 通信协议（串口）

- 物理：USB-CDC 或 UART，**建议 460800 或 921600**（高速巡航时状态包更密；115200 仅调试）
- 帧格式建议：`[0xAA][len][type][payload...][crc8]`
- 上行（ESP32 → Pi）：位姿、速度、机构状态、**ToF/超声距离数组**、错误码
- 下行（Pi → ESP32）：
  - `SET_VELOCITY`（线速度 / 角速度）
  - `MOVE_TO`（相对/世界目标，下位机做简单底层也可由上位机拆成速度）
  - `ROLLER_ON` / `ROLLER_OFF` / `ROLLER_SPEED`
  - `BALL_COUNT` 查询（可选）
  - `RETURN_ODOM_RESET`（起点清零）
  - `ESTOP`
- **超时看门狗**：上位机指令超过 N ms 未刷新，下位机自动减速停车

第一期可先做 `SET_VELOCITY` + 状态上报，由上位机闭环到目标点，协议更简单。

---

## 8. 软件模块划分

### 上位机（Python + OpenCV）

| 模块 | 职责 |
|------|------|
| `camera` | 采集、分辨率、ROI |
| `detector` | 多 HSV + 圆度 + 尺寸 + 连续帧，输出球列表 |
| `tracker` | 帧间 ID 关联、置信度、丢失淘汰 |
| `localizer` | 接收里程计，维护 `(x,y,θ)` |
| `range_safety` | 融合 ToF 距离，计算 `v_allowed`，下发限速 |
| `ball_range` | 单目像素直径 → 球距、相对入口偏差 |
| `planner` | 目标选择、接近点、**RETURN 信标搜索策略** |
| `beacon` | 信标 HSV + AprilTag 检测、standoff 到位判据 |
| `fsm` | 总状态机（含 R1–R4 / SAFE_STOP） |
| `serial_link` | 收发协议、心跳 |
| `config` | HSV、几何阈值、机械参数 YAML |

### 下位机（Arduino / ESP-IDF）

| 模块 | 职责 |
|------|------|
| `motor` | **无刷** PWM+DIR、方向、速度环 |
| `encoder` | 后驱计数；里程计=驱动轴+前轮转角 |
| `roller` | 滚轮启停、转速（名义≈450RPM，筋尖Ø152）、堵转保护 |
| `sonar` | ToF 主 + 超声辅；滤波与急停阈值 |
| `protocol` | 帧解析与应答 |
| `safety` | 看门狗、急停 |

---

## 9. 供电

```text
锂电池组
  ├─ 电机驱动（大电流，注意地线共地）
  └─ 稳压 5V
        ├─ Orange Pi Zero 2W
        └─ ESP32（若与 Pi USB 供电，注意共地与电流）
摄像头由 Pi USB 供电
```

- 电机与逻辑**共地**
- 预留电流余量，避免电机堵转导致上位机重启
- 建议分电源开关：逻辑先上电，电机后上电

---

## 10. 风险与取舍

| 风险 | 影响 | 缓解 |
|------|------|------|
| 色调干扰残留误检 | 空跑、撞错目标 | 圆度+尺寸+连续帧；HSV 现场标定 |
| **撞墙/围网** | **硬性不可接受** | **多路 ToF 本地急停 + v_allowed；装配遮挡检查；实测 D_hard** |
| ToF 对网/深色失效 | 误判远距离 | 多路+滤波；异常保守降速；可加超声冗余 |
| 里程计漂移 | 回不到精确起点 | 低速、好轮胎；二期绝对定位 |
| Zero 2W 性能/摄像头兼容 | 帧率不足 | 降分辨率；换 UVC 头；升级 3B |
| 打滑/草地 | 转弯半径失控、里程计漂 | 轮胎选型；限制加速度；高速段降角速度 |
| 高速漏捡 | 球从滚轮旁掠过 | 瞄准入口投影；降低过球相对速度；失败重扫 |
| 滚轮堵转/缠草 | 机构失效 | 电流检测反转；入口导流与间隙按球径（约 6.7 cm）设计 |
| 单机故障 | 任务中断 | 下位机看门狗停车；状态可恢复 |

---

## 11. 开发里程碑（建议）

| 阶段 | 内容 | 验收 |
|------|------|------|
| M1 机械+底盘 | 车架、**无刷**后驱+编码、舵机转向、ESP32 速度环 | 直线/原地转稳定 |
| M2 串口链路 | 协议、心跳、看门狗、里程计 + **ToF 距离上报** | Pi 可遥控、读位姿与距离 |
| M2.5 防撞 | D_hard 急停、v_allowed 钳制、侧向限转 | **全速段冲墙试验均能停住** |
| M3 单球视觉 | 检测器 + **单目测距标定** + 标定工具 | 静态下稳定识别现场网球并估距 |
| M4 接近与捡球 | 筋尖进料+后挡咬合 + **450RPM 抛高** + VERIFY | 单球不停机捡取并入筐 |
| M5 多球与回起点 | 贪心/串联 + **信标 R1–R4** | 清场后停在信标 **standoff≥35cm** |
| M5.5 提速 | 巡航 1.2–1.5 m/s、优化串口/帧率 | 全程平均用时明显下降 |
| M6 稳健性 | 避障、丢球重搜、**信标误检/倒柱 SAFE_STOP** | 干扰物与变光下可用 |

---

## 12. 明确不做的（第一期）

- 不上树莓派、不上 YOLO/大模型
- **第一期不上深度相机/双目立体**（ToF/超声 + 单目已覆盖避障与球距）
- 不做 SLAM、多机协同
- 不追求厘米级绝对定位
- 不做云端视觉

---

## 13. 下一步

1. 确认机械形态：**前轮转向 + 后轮单电机通轴** + 前滚轮×弧板收集 + **样品筐标称 30 球（成品再分级加高）**；ToF 安装位避开车体遮挡  
2. 确认主要场地（室内硬地 / 室外硬地 / 塑胶）、围网材质、目标空跑速度  
3. 按 M1 → M2 → **M2.5 防撞** → 视觉与滚轮抛送标定（名义 **≈450 RPM**，筋尖基准）  

---

*本方案版本：v1.3c · 工程 E1.3c · 无刷×2；挡板梁 y=520；筋尖Ø152/450RPM；进料后挡分段；信标 standoff≥35cm；样品 30 球；容量成品分级。*
